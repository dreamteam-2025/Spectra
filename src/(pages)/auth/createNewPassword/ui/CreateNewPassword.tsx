"use client";

type Props = {
  token?: string | string[] | undefined;
};

export const CreateNewPassword = ({ token }: Props) => {
  //console.log(token);

  return (
    <main>
      <p>Что-то</p>
      {/* <div>Token: {token}</div> */}
    </main>
  );
};
